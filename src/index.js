import React from 'react';
import ReactDOM from 'react-dom/client'; // Changez ici
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); // Changez ici
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);